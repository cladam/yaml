# yaml

## Usage

```sh
hica build   # compile to binary
hica run     # compile and run
hica fmt     # format .hc file
hica check   # type-check without emitting
hica clean   # remove generated files
```

// Generated by hica (test mode)
module test_block_collections

import yaml_types
import scalar
import parser
import api

extern hica-exit( code : int ) : a
  c inline "(exit(kk_integer_clamp32(#1, kk_context())), kk_box_null())"

fun hctest0()
  val input = "name: my-project\nversion: 1"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "name")
        Just(v) ->
          (if !((hc_yaml_str(v) == Just("my-project"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
      match hc_yaml_get(doc, "version")
        Just(v) ->
          (if !((hc_yaml_int(v) == Just(1))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest1()
  val input = "enabled: true\ndata: null"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "enabled")
        Just(v) ->
          (if !((hc_yaml_bool(v) == Just(True))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
      match hc_yaml_get(doc, "data")
        Just(YNull) ->
          (if !(True) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest2()
  val input = "database:\n  host: localhost\n  port: 5432"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "database")
        Just(inner) ->
          match hc_yaml_get(inner, "host")
            Just(v) ->
              (if !((hc_yaml_str(v) == Just("localhost"))) then throw("assertion failed") else ())
            _ ->
              (if !(False) then throw("assertion failed") else ())
          match hc_yaml_get(inner, "port")
            Just(v) ->
              (if !((hc_yaml_int(v) == Just(5432))) then throw("assertion failed") else ())
            _ ->
              (if !(False) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest3()
  val input = "# this is a comment\nname: test\n# another comment\nport: 80"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "name")
        Just(v) ->
          (if !((hc_yaml_str(v) == Just("test"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
      match hc_yaml_get(doc, "port")
        Just(v) ->
          (if !((hc_yaml_int(v) == Just(80))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest4()
  val input = "name: test # inline comment\nport: 80"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "name")
        Just(YStr(s)) ->
          (if !((s == "test")) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest5()
  val input = "host: localhost\nport: 5432"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "host")
        Just(v) ->
          (if !((hc_yaml_str(v) == Just("localhost"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
      match hc_yaml_get(doc, "port")
        Just(v) ->
          (if !((hc_yaml_int(v) == Just(5432))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
      (if !(hc_yaml_get(doc, "missing").is-nothing) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest6()
  val input = "- apple\n- banana\n- cherry"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_list(doc)
        Just(items) ->
          (if !((list/length(items) == 3)) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[0].unjust) == Just("apple"))) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[1].unjust) == Just("banana"))) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[2].unjust) == Just("cherry"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest7()
  val input = "- 1\n- 2\n- 3"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_list(doc)
        Just(items) ->
          (if !((list/length(items) == 3)) then throw("assertion failed") else ())
          (if !((hc_yaml_int(items[0].unjust) == Just(1))) then throw("assertion failed") else ())
          (if !((hc_yaml_int(items[1].unjust) == Just(2))) then throw("assertion failed") else ())
          (if !((hc_yaml_int(items[2].unjust) == Just(3))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest8()
  val input = "- hello\n- 42\n- true\n- null"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_list(doc)
        Just(items) ->
          (if !((list/length(items) == 4)) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[0].unjust) == Just("hello"))) then throw("assertion failed") else ())
          (if !((hc_yaml_int(items[1].unjust) == Just(42))) then throw("assertion failed") else ())
          (if !((hc_yaml_bool(items[2].unjust) == Just(True))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest9()
  val input = "- only"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_list(doc)
        Just(items) ->
          (if !((list/length(items) == 1)) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[0].unjust) == Just("only"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest10()
  val input = "# fruits\n- apple\n# more\n- banana"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_list(doc)
        Just(items) ->
          (if !((list/length(items) == 2)) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[0].unjust) == Just("apple"))) then throw("assertion failed") else ())
          (if !((hc_yaml_str(items[1].unjust) == Just("banana"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest11()
  val input = "fruits:\n  - apple\n  - banana"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "fruits")
        Just(v) ->
          match hc_yaml_list(v)
            Just(items) ->
              (if !((list/length(items) == 2)) then throw("assertion failed") else ())
              (if !((hc_yaml_str(items[0].unjust) == Just("apple"))) then throw("assertion failed") else ())
              (if !((hc_yaml_str(items[1].unjust) == Just("banana"))) then throw("assertion failed") else ())
            _ ->
              (if !(False) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun hctest12()
  val input = "ignore:\n- foo\n- bar\nname: test"
  match hc_yaml_parse(input)
    Right(doc) ->
      match hc_yaml_get(doc, "ignore")
        Just(v) ->
          match hc_yaml_list(v)
            Just(items) ->
              (if !((list/length(items) == 2)) then throw("assertion failed") else ())
              (if !((hc_yaml_str(items[0].unjust) == Just("foo"))) then throw("assertion failed") else ())
              (if !((hc_yaml_str(items[1].unjust) == Just("bar"))) then throw("assertion failed") else ())
            _ ->
              (if !(False) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
      match hc_yaml_get(doc, "name")
        Just(v) ->
          (if !((hc_yaml_str(v) == Just("test"))) then throw("assertion failed") else ())
        _ ->
          (if !(False) then throw("assertion failed") else ())
    _ ->
      (if !(False) then throw("assertion failed") else ())

fun main()
  println("")
  println("running 13 test(s)...")
  println("")
  val r0 = match try({ hctest0() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "simple map")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "simple map")
      println("    " ++ e.message)
      False
  val r1 = match try({ hctest1() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "map with bool and null")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "map with bool and null")
      println("    " ++ e.message)
      False
  val r2 = match try({ hctest2() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "nested map")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "nested map")
      println("    " ++ e.message)
      False
  val r3 = match try({ hctest3() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "map with comments")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "map with comments")
      println("    " ++ e.message)
      False
  val r4 = match try({ hctest4() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "map with inline comment")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "map with inline comment")
      println("    " ++ e.message)
      False
  val r5 = match try({ hctest5() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "yaml_get on map")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "yaml_get on map")
      println("    " ++ e.message)
      False
  val r6 = match try({ hctest6() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "simple list of strings")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "simple list of strings")
      println("    " ++ e.message)
      False
  val r7 = match try({ hctest7() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "list of integers")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "list of integers")
      println("    " ++ e.message)
      False
  val r8 = match try({ hctest8() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "list of mixed scalars")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "list of mixed scalars")
      println("    " ++ e.message)
      False
  val r9 = match try({ hctest9() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "single item list")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "single item list")
      println("    " ++ e.message)
      False
  val r10 = match try({ hctest10() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "list with comments")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "list with comments")
      println("    " ++ e.message)
      False
  val r11 = match try({ hctest11() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "map with list value")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "map with list value")
      println("    " ++ e.message)
      False
  val r12 = match try({ hctest12() })
    Ok(_) ->
      println("  \x1b[32m\u2713\x1b[0m " ++ "same-indent list under map key")
      True
    Error(e) ->
      println("  \x1b[31m\u2717\x1b[0m " ++ "same-indent list under map key")
      println("    " ++ e.message)
      False
  println("")
  val passed = [r0, r1, r2, r3, r4, r5, r6, r7, r8, r9, r10, r11, r12].filter(fn(x) x).length
  val failed = 13 - passed
  if failed == 0 then
    println("\x1b[32m" ++ passed.show ++ " test(s) passed\x1b[0m")
  else
    println("\x1b[31m" ++ failed.show ++ " test(s) failed\x1b[0m, " ++ passed.show ++ " passed")
    hica-exit(1)

